"""Application configurations for the simple_pages app."""

from django.apps import AppConfig


class SimplePagesConfig(AppConfig):
    """SimplePages application config."""

    name = "simple_pages"
    verbose_name = "Simple Pages"
